import { FamilyHomeDashboard } from "@/components/family-home-dashboard";

/** Fast first paint: dashboard hydrates client-side from /api/family/home. */
export default function FamilyHomePage() {
  return <FamilyHomeDashboard />;
}
